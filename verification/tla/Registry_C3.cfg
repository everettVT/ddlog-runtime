\* C3: operator deletes the lock file of a live writer; the old writer's
\* Drop then deletes the new writer's lock.  NoLostUpdate expected to FAIL.
CONSTANTS
    Writers = {w1, w2}
    Importers = {}
    Readers = {}
    Vals = {v1, v2}
    InitVal = v1
    InitId = a
    ImportId = b
    CreateIds = {}
    OpIds = {a}
    ReadIds = {}
    SrcV1 = v1
    SrcV2 = v2
    SrcCur = v2
    MaxRev = 2
    MaxCrashes = 0
    UnsafeOperator = TRUE
    None = None
SPECIFICATION Spec
VIEW View
INVARIANTS
    NoLostUpdate
CHECK_DEADLOCK FALSE
