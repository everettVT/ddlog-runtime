\* C4: a crash between the event write (412) and the pointer write (413).
\* NoOrphanEvent FAILS (reachable); OrphanEventuallyResolved FAILS under
\* weak fairness; OrphanBlocksTransitions HOLDS.  No VIEW (liveness).
CONSTANTS
    Writers = {w1}
    Importers = {}
    Readers = {}
    Vals = {v1, v2}
    InitVal = v1
    InitId = a
    ImportId = b
    CreateIds = {}
    OpIds = {a}
    ReadIds = {}
    SrcV1 = v1
    SrcV2 = v2
    SrcCur = v2
    MaxRev = 2
    MaxCrashes = 1
    UnsafeOperator = FALSE
    None = None
SPECIFICATION FairSpec
PROPERTIES
    OrphanEventuallyResolved
CHECK_DEADLOCK FALSE
